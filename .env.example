# Leave empty to use Vite /api proxy in local dev
VITE_API_BASE_URL=

# Where Vite forwards /api requests during `npm run dev`
VITE_BACKEND_PROXY_TARGET=http://127.0.0.1:8000

# Used by backend/api.py if exported in backend shell
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
